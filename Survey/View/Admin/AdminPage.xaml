<Page x:Class="Survey.View.Admin.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Survey.View.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">
    <Page.Resources>
        <Style x:Key="LabelTextBlock" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="InputTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>


    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
            Grid.Row="1"
            Grid.Column="2" >
                <Button x:Name="Users" 
                   
                HorizontalAlignment="Center"
                HorizontalContentAlignment="Center"
                Margin="3"
                Click="Users_Click">Сотрудники
                </Button>
                <Button  x:Name="Surveys"    
                HorizontalAlignment="Center"
                HorizontalContentAlignment="Center"
                Margin="3"
                Click="Surveys_Click"  Content="Опросы"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <DataGrid Grid.Row="2" Grid.Column=" 0"                       
                      Margin="3">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding Path=Surname}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Path=Name}"  Width="*"/>
                    <DataGridTextColumn Header="Рейтингв %" Binding="{Binding Path=RatingEmployee}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" ShowsPreview="False" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

            <DataGrid Grid.Row="2" Grid.Column="2"
                      Margin="3">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Тест" Binding="{Binding Path=Survey}" Width="*"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding Path=Catalog}" Width="100"/>
                    <DataGridTextColumn Header="Итого в %" Binding="{Binding Path=RatingSurvey}" Width="*"/>
                </DataGrid.Columns>
                
            </DataGrid> 

        </Grid>

        <Button x:Name="Exit" 
                Grid.Row="1"
                Grid.Column="0"    
                HorizontalAlignment="Left"
                HorizontalContentAlignment="Center"
                Margin="3"
                Click="Exit_Click">Выход
        </Button>
        


        <Grid Grid.Row="1" Grid.Column="1">

            <TextBlock HorizontalAlignment="Center">Статистика</TextBlock>
            

        </Grid>
    </Grid>
</Page>
